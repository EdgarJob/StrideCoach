# Copy this file to .env in the root directory
# Then fill in your actual credentials

# Supabase Configuration
# Get these from your Supabase project dashboard: Settings → API
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Optional local AI provider for development
# Start LM Studio's local server first, then set:
# EXPO_PUBLIC_AI_PROVIDER=lmstudio
# EXPO_PUBLIC_LOCAL_AI_BASE_URL=http://127.0.0.1:8787/v1
# EXPO_PUBLIC_LOCAL_AI_MODEL=qwen/qwen3.5-35b-a3b
#
# For Expo web, run the local bridge as well:
# npm run local-ai-proxy
# It forwards browser-safe requests to LM Studio at http://127.0.0.1:1234/v1.

# Note: OpenAI API key is now stored server-side in Supabase Edge Functions
# You do NOT need to set it here in the client app
# Set it in Supabase dashboard: Edge Functions → Settings → Secrets
# - OPENAI_API_KEY=your-openai-key (server-side only)
# - OPENAI_MODEL=gpt-4o-mini (server-side only)

# Optional: Sentry DSN for error logging (production only)
# EXPO_PUBLIC_SENTRY_DSN=your-sentry-dsn-here
